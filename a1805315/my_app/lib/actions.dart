import 'package:flutter/material.dart';

class Actions extends StatelessWidget {
  final String actionName;
  final Function handleClick;

  Actions(this.actionName,this.handleClick);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
